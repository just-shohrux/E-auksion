import React from 'react';
import LotDetailContainer from "../containers/LotDetailContainer";

const LotDetailPage = (props) => {
    return (
        <>
            <LotDetailContainer {...props}/>
        </>
    );
};

export default LotDetailPage;
